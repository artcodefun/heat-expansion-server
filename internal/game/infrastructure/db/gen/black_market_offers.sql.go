// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: black_market_offers.sql

package gen

import (
	"context"
	"database/sql"
)

const getBlackMarketOfferByID = `-- name: GetBlackMarketOfferByID :one

SELECT id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
FROM game.black_market_offers
WHERE id = $1
`

// Black market offers queries
func (q *Queries) GetBlackMarketOfferByID(ctx context.Context, id int64) (BlackMarketOffer, error) {
	row := q.queryRow(ctx, q.getBlackMarketOfferByIDStmt, getBlackMarketOfferByID, id)
	var i BlackMarketOffer
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.PrototypeID,
		&i.PriceInCrystals,
		&i.EndsAt,
		&i.IsLimited,
		&i.Priority,
	)
	return i, err
}

const getBlackMarketOfferByIDForUpdate = `-- name: GetBlackMarketOfferByIDForUpdate :one
SELECT id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
FROM game.black_market_offers
WHERE id = $1
FOR UPDATE
`

func (q *Queries) GetBlackMarketOfferByIDForUpdate(ctx context.Context, id int64) (BlackMarketOffer, error) {
	row := q.queryRow(ctx, q.getBlackMarketOfferByIDForUpdateStmt, getBlackMarketOfferByIDForUpdate, id)
	var i BlackMarketOffer
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.PrototypeID,
		&i.PriceInCrystals,
		&i.EndsAt,
		&i.IsLimited,
		&i.Priority,
	)
	return i, err
}

const insertBlackMarketOffer = `-- name: InsertBlackMarketOffer :one
INSERT INTO game.black_market_offers (
    kind,
    prototype_id,
    price_in_crystals,
    ends_at,
    is_limited,
    priority
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6
)
RETURNING id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
`

type InsertBlackMarketOfferParams struct {
	Kind            string        `json:"kind"`
	PrototypeID     int64         `json:"prototype_id"`
	PriceInCrystals int64         `json:"price_in_crystals"`
	EndsAt          sql.NullInt64 `json:"ends_at"`
	IsLimited       bool          `json:"is_limited"`
	Priority        int64         `json:"priority"`
}

func (q *Queries) InsertBlackMarketOffer(ctx context.Context, arg InsertBlackMarketOfferParams) (BlackMarketOffer, error) {
	row := q.queryRow(ctx, q.insertBlackMarketOfferStmt, insertBlackMarketOffer,
		arg.Kind,
		arg.PrototypeID,
		arg.PriceInCrystals,
		arg.EndsAt,
		arg.IsLimited,
		arg.Priority,
	)
	var i BlackMarketOffer
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.PrototypeID,
		&i.PriceInCrystals,
		&i.EndsAt,
		&i.IsLimited,
		&i.Priority,
	)
	return i, err
}

const listActiveLimitedBlackMarketOffers = `-- name: ListActiveLimitedBlackMarketOffers :many
SELECT id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
FROM game.black_market_offers
WHERE is_limited = TRUE
    AND ends_at > $1
ORDER BY priority DESC, id ASC
`

func (q *Queries) ListActiveLimitedBlackMarketOffers(ctx context.Context, now sql.NullInt64) ([]BlackMarketOffer, error) {
	rows, err := q.query(ctx, q.listActiveLimitedBlackMarketOffersStmt, listActiveLimitedBlackMarketOffers, now)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BlackMarketOffer{}
	for rows.Next() {
		var i BlackMarketOffer
		if err := rows.Scan(
			&i.ID,
			&i.Kind,
			&i.PrototypeID,
			&i.PriceInCrystals,
			&i.EndsAt,
			&i.IsLimited,
			&i.Priority,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listExpiredLimitedBlackMarketOffers = `-- name: ListExpiredLimitedBlackMarketOffers :many
SELECT id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
FROM game.black_market_offers
WHERE is_limited = TRUE
    AND ends_at <= $1
ORDER BY ends_at ASC, priority DESC, id ASC
`

func (q *Queries) ListExpiredLimitedBlackMarketOffers(ctx context.Context, now sql.NullInt64) ([]BlackMarketOffer, error) {
	rows, err := q.query(ctx, q.listExpiredLimitedBlackMarketOffersStmt, listExpiredLimitedBlackMarketOffers, now)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BlackMarketOffer{}
	for rows.Next() {
		var i BlackMarketOffer
		if err := rows.Scan(
			&i.ID,
			&i.Kind,
			&i.PrototypeID,
			&i.PriceInCrystals,
			&i.EndsAt,
			&i.IsLimited,
			&i.Priority,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateBlackMarketOffer = `-- name: UpdateBlackMarketOffer :one
UPDATE game.black_market_offers
SET kind = $1,
    prototype_id = $2,
    price_in_crystals = $3,
    ends_at = $4,
    is_limited = $5,
    priority = $6
WHERE id = $7
RETURNING id, kind, prototype_id, price_in_crystals, ends_at, is_limited, priority
`

type UpdateBlackMarketOfferParams struct {
	Kind            string        `json:"kind"`
	PrototypeID     int64         `json:"prototype_id"`
	PriceInCrystals int64         `json:"price_in_crystals"`
	EndsAt          sql.NullInt64 `json:"ends_at"`
	IsLimited       bool          `json:"is_limited"`
	Priority        int64         `json:"priority"`
	ID              int64         `json:"id"`
}

func (q *Queries) UpdateBlackMarketOffer(ctx context.Context, arg UpdateBlackMarketOfferParams) (BlackMarketOffer, error) {
	row := q.queryRow(ctx, q.updateBlackMarketOfferStmt, updateBlackMarketOffer,
		arg.Kind,
		arg.PrototypeID,
		arg.PriceInCrystals,
		arg.EndsAt,
		arg.IsLimited,
		arg.Priority,
		arg.ID,
	)
	var i BlackMarketOffer
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.PrototypeID,
		&i.PriceInCrystals,
		&i.EndsAt,
		&i.IsLimited,
		&i.Priority,
	)
	return i, err
}
